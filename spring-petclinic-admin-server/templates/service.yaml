apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-petclinic-admin-server.fullname" . }}
  labels:
    {{- include "spring-petclinic-admin-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spring-petclinic-admin-server.selectorLabels" . | nindent 4 }}
